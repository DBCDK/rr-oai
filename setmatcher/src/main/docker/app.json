{
    "appName": "rawrepo-oai-setmatcher",
    "jdbc": {
        "jdbc/rawrepo-oai-migrate": {
            "xa": false,
            "url": "${RAWREPO_OAI_POSTGRES_URL}",
            "maxSize": "2",
            "steadySize": "0"
        },
        "jdbc/rawrepo-oai": {
            "xa": true,
            "url": "${RAWREPO_OAI_POSTGRES_URL}",
            "maxSize": "${MAX_POOL_SIZE}"
        },
        "jdbc/rawrepo": {
            "xa": true,
            "url": "${RAWREPO_POSTGRES_URL}",
            "maxSize": "${MAX_POOL_SIZE}"
        }
    }
}

