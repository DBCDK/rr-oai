{
    "appName": "rawrepo-oai-service",
    "jdbc": {
        "jdbc/rawrepo-oai": {
            "readOnly": "true",
            "url": "${RAWREPO_OAI_POSTGRES_URL}",
            "maxSize": "${MAX_POOL_SIZE}"
        }
    }
}
